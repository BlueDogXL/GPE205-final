using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MagicalTarget : MonoBehaviour
{
    // essentially the idea is that our homing script can just look for objects with this component instead of pawns
    // i still don't quite know how i'm gonna do the actual act of homing but this way i can test it on random cubes or whatever
}
